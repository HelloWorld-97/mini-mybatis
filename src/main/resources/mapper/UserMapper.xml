<mapper namespace="com.danzz.dao.UserQueryDao">
    <select id="queryUserById" parameterType="java.lang.Long" resultType="com.danzz.entity.User">
        SELECT id,name from t_user
        where id = #{id};
    </select>
</mapper>